export class SvgModel {
    public amount: number;
    public percentage: string;
    constructor(amount: number, percentage: string) {
        this.amount = amount;
        this.percentage = percentage;
    }
}
